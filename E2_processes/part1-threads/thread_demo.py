"""
================================================================================
Part 1: Thread Demo - SOLUTION
================================================================================

This module demonstrates fundamental threading concepts:
- Thread creation and execution
- Shared memory between threads
- Race conditions and how to prevent them
- Context switching and concurrency benefits

================================================================================
WHAT IS A THREAD?
================================================================================

A thread is a lightweight unit of execution within a process.
Multiple threads share the same memory space but have separate execution stacks.

    ┌─────────────────────────────────────────────────────────────────────────┐
    │                            PROCESS                                      │
    │                                                                         │
    │   ┌─────────────────────────────────────────────────────────────────┐   │
    │   │                     SHARED MEMORY                               │   │
    │   │                                                                 │   │
    │   │   • Global variables                                           │   │
    │   │   • Heap (dynamically allocated objects)                       │   │
    │   │   • Code (instructions)                                        │   │
    │   │   • Open file descriptors                                      │   │
    │   │                                                                 │   │
    │   └─────────────────────────────────────────────────────────────────┘   │
    │                                                                         │
    │   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐               │
    │   │  Thread 0   │    │  Thread 1   │    │  Thread 2   │               │
    │   │             │    │             │    │             │               │
    │   │ ┌─────────┐ │    │ ┌─────────┐ │    │ ┌─────────┐ │               │
    │   │ │  Stack  │ │    │ │  Stack  │ │    │ │  Stack  │ │               │
    │   │ │ (local) │ │    │ │ (local) │ │    │ │ (local) │ │               │
    │   │ └─────────┘ │    │ └─────────┘ │    │ └─────────┘ │               │
    │   │             │    │             │    │             │               │
    │   │ Registers   │    │ Registers   │    │ Registers   │               │
    │   │ Program Ctr │    │ Program Ctr │    │ Program Ctr │               │
    │   └─────────────┘    └─────────────┘    └─────────────┘               │
    │                                                                         │
    │   PRIVATE to each thread:        SHARED between threads:               │
    │   • Stack (local variables)      • Heap (objects, lists, dicts)       │
    │   • Registers                    • Global variables                    │
    │   • Program counter              • Code                                │
    │                                                                         │
    └─────────────────────────────────────────────────────────────────────────┘

================================================================================
THREADS vs PROCESSES
================================================================================

    ┌─────────────────────────────────┬─────────────────────────────────┐
    │           THREADS               │           PROCESSES             │
    ├─────────────────────────────────┼─────────────────────────────────┤
    │                                 │                                 │
    │  Share memory (fast!)           │  Separate memory (safe!)        │
    │  ┌───────────────────────┐      │  ┌───────────┐ ┌───────────┐   │
    │  │   Shared Heap         │      │  │  Memory   │ │  Memory   │   │
    │  │  ┌─────┐ ┌─────┐     │      │  │  Space 1  │ │  Space 2  │   │
    │  │  │ T1  │ │ T2  │     │      │  │           │ │           │   │
    │  │  └─────┘ └─────┘     │      │  │    P1     │ │    P2     │   │
    │  └───────────────────────┘      │  └───────────┘ └───────────┘   │
    │                                 │                                 │
    │  Lightweight creation           │  Heavyweight creation           │
    │  (~microseconds)                │  (~milliseconds)                │
    │                                 │                                 │
    │  Risk: Race conditions          │  Safe: No shared state          │
    │  Need: Locks, synchronization   │  Need: IPC (pipes, sockets)     │
    │                                 │                                 │
    │  Best for: I/O-bound tasks      │  Best for: CPU-bound tasks      │
    │                                 │  (avoids Python GIL)            │
    │                                 │                                 │
    └─────────────────────────────────┴─────────────────────────────────┘

================================================================================
WHY USE THREADS?
================================================================================

    PROBLEM: Blocking I/O wastes CPU time
    ─────────────────────────────────────

    Single-threaded (BLOCKING):
    ┌──────────────────────────────────────────────────────────────────────────┐
    │                                                                          │
    │  Time ──────────────────────────────────────────────────────────────►   │
    │                                                                          │
    │  Task 1: ████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░   │
    │                     ▲                                                    │
    │                     │ Waiting for I/O (CPU idle!)                       │
    │                                                                          │
    │  Task 2:            ░░░░░░░░░░░░░░░░░░████████████░░░░░░░░░░░░░░░░░░░░   │
    │                                                  ▲                       │
    │                                                  │ Waiting again         │
    │                                                                          │
    │  Task 3:                                         ░░░░░░░░░████████████   │
    │                                                                          │
    │  Total time: ═══════════════════════════════════════════════════════    │
    │              (3 seconds if each I/O takes 1 second)                     │
    │                                                                          │
    └──────────────────────────────────────────────────────────────────────────┘

    Multi-threaded (CONCURRENT):
    ┌──────────────────────────────────────────────────────────────────────────┐
    │                                                                          │
    │  Time ──────────────────────────────────────────────────────────────►   │
    │                                                                          │
    │  Thread 1: ████████████                                                  │
    │  Thread 2: ████████████     All waiting concurrently!                   │
    │  Thread 3: ████████████                                                  │
    │                                                                          │
    │  Total time: ════════════                                               │
    │              (1 second - 3x faster!)                                    │
    │                                                                          │
    └──────────────────────────────────────────────────────────────────────────┘

================================================================================
"""

import threading
import time
from concurrent.futures import ThreadPoolExecutor


# =============================================================================
# Demo 1: Basic Thread Creation
# =============================================================================

def worker_function(worker_id, shared_data):
    """
    Simple worker that modifies shared data.
    
    This function runs in a separate thread but can access
    and modify the shared_data list because threads share memory!
    
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                    WORKER FUNCTION EXECUTION                            │
    ├─────────────────────────────────────────────────────────────────────────┤
    │                                                                         │
    │   Main Thread                    Worker Thread                          │
    │       │                              │                                  │
    │       │  creates thread              │                                  │
    │       │  with shared_data ──────────►│                                  │
    │       │                              │                                  │
    │       │                              │ print("Starting")                │
    │       │                              │ sleep(0.1)                       │
    │       │                              │ shared_data.append(...)          │
    │       │                              │       │                          │
    │       │                              │       ▼                          │
    │       │                         ┌────────────────────┐                  │
    │       │                         │   SHARED MEMORY    │                  │
    │       │                         │   shared_data = [  │                  │
    │       │                         │     "Result from   │                  │
    │       │                         │      thread N"     │                  │
    │       │                         │   ]                │                  │
    │       │                         └────────────────────┘                  │
    │       │                              │                                  │
    │       │◄─────── join() ──────────────│ (thread completes)              │
    │       │                                                                 │
    │       ▼                                                                 │
    │   Access shared_data                                                    │
    │   (sees modifications!)                                                 │
    │                                                                         │
    └─────────────────────────────────────────────────────────────────────────┘
    """
    print(f"[Thread {worker_id}] Starting")
    
    # Simulate some work
    time.sleep(0.1)
    
    # Access shared data (threads share memory!)
    # This list was created in the main thread but we can modify it here
    shared_data.append(f"Result from thread {worker_id}")
    
    print(f"[Thread {worker_id}] Finished")


def demo_basic_threads():
    """
    Show basic thread creation and shared memory.
    
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                    THREAD CREATION PATTERN                              │
    ├─────────────────────────────────────────────────────────────────────────┤
    │                                                                         │
    │   STEP 1: Create threads                                               │
    │   ──────────────────────                                               │
    │                                                                         │
    │       for i in range(3):                                               │
    │           t = threading.Thread(target=func, args=(i, data))            │
    │                           │              │            │                │
    │                           │              │            └── Arguments    │
    │                           │              └── Function to run           │
    │                           └── Create thread object                     │
    │                                                                         │
    │   STEP 2: Start threads                                                │
    │   ─────────────────────                                                │
    │                                                                         │
    │       t.start()  ──► Thread begins executing target function           │
    │                                                                         │
    │   STEP 3: Wait for completion                                          │
    │   ───────────────────────────                                          │
    │                                                                         │
    │       t.join()   ──► Block until thread finishes                       │
    │                                                                         │
    │                                                                         │
    │   EXECUTION TIMELINE:                                                  │
    │   ───────────────────                                                  │
    │                                                                         │
    │   Main:     ──[create]──[start]──[start]──[start]──[join]──[join]──►  │
    │                           │        │        │        ▲       ▲        │
    │   Thread 0:               └───────────────────────────┘       │        │
    │   Thread 1:                        └──────────────────────────┘        │
    │   Thread 2:                                 └─────────────────────►    │
    │                                                                         │
    │   All threads run CONCURRENTLY after start()!                          │
    │                                                                         │
    └─────────────────────────────────────────────────────────────────────────┘
    """
    print("\n" + "="*60)
    print("Demo 1: Basic Threads with Shared Memory")
    print("="*60)
    
    shared_data = []  # This list is shared between all threads
    threads = []
    
    # Create and start threads
    for i in range(3):
        t = threading.Thread(target=worker_function, args=(i, shared_data))
        threads.append(t)
        t.start()  # Thread begins execution
    
    # Wait for all threads to complete
    # join() blocks until the thread terminates
    for t in threads:
        t.join()
    
    print(f"\nShared data after all threads: {shared_data}")
    print("Note: All threads accessed the SAME list!")


# =============================================================================
# Demo 2: Race Condition
# =============================================================================

class UnsafeCounter:
    """
    Counter WITHOUT thread safety - will have race conditions.
    
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                      RACE CONDITION EXPLAINED                           │
    ├─────────────────────────────────────────────────────────────────────────┤
    │                                                                         │
    │   The increment operation self.count += 1 is NOT atomic!               │
    │   It actually consists of THREE separate operations:                   │
    │                                                                         │
    │       1. READ:   temp = self.count    (load from memory)               │
    │       2. MODIFY: temp = temp + 1      (add one)                        │
    │       3. WRITE:  self.count = temp    (store to memory)                │
    │                                                                         │
    │   A context switch can happen BETWEEN any of these steps!              │
    │                                                                         │
    │   RACE CONDITION SCENARIO:                                             │
    │   ─────────────────────────                                            │
    │                                                                         │
    │   Initial: self.count = 5                                              │
    │                                                                         │
    │   Thread A              Thread B              self.count               │
    │   ────────              ────────              ──────────               │
    │   READ (5)                                        5                    │
    │                         READ (5)                  5                    │
    │   MODIFY (6)                                      5                    │
    │                         MODIFY (6)                5                    │
    │   WRITE (6)                                       6                    │
    │                         WRITE (6)                 6  ← Should be 7!   │
    │                                                                         │
    │   Expected: 7 (two increments)                                         │
    │   Actual:   6 (one increment LOST!)                                    │
    │                                                                         │
    │   This is called a "lost update" - a classic race condition.           │
    │                                                                         │
    └─────────────────────────────────────────────────────────────────────────┘
    """
    
    def __init__(self):
        self.count = 0
    
    def increment(self):
        # This is NOT atomic! Read-modify-write can be interrupted
        #
        #   ┌───────────────────────────────────────────────────────────┐
        #   │  DANGER: These 3 steps can be interrupted!               │
        #   │                                                           │
        #   │  current = self.count    ← Step 1: READ                  │
        #   │  ─── CONTEXT SWITCH POSSIBLE HERE! ───                   │
        #   │  current = current + 1   ← Step 2: MODIFY (in CPU)       │
        #   │  ─── CONTEXT SWITCH POSSIBLE HERE! ───                   │
        #   │  self.count = current    ← Step 3: WRITE                 │
        #   │                                                           │
        #   └───────────────────────────────────────────────────────────┘
        #
        current = self.count
        time.sleep(0.0001)  # Simulate processing / increase chance of switch
        self.count = current + 1


class SafeCounter:
    """
    Counter WITH thread safety using a lock.
    
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                      LOCK (MUTEX) EXPLAINED                             │
    ├─────────────────────────────────────────────────────────────────────────┤
    │                                                                         │
    │   A lock ensures MUTUAL EXCLUSION - only one thread can hold           │
    │   the lock at a time. Other threads must WAIT.                         │
    │                                                                         │
    │   WITH LOCK (SAFE):                                                    │
    │   ─────────────────                                                    │
    │                                                                         │
    │   Thread A              Thread B              self.count               │
    │   ────────              ────────              ──────────               │
    │   acquire lock ✓                                  5                    │
    │   READ (5)              acquire lock ⏳           5                    │
    │   MODIFY (6)            (waiting...)              5                    │
    │   WRITE (6)             (waiting...)              6                    │
    │   release lock          (waiting...)              6                    │
    │                         acquire lock ✓            6                    │
    │                         READ (6)                  6                    │
    │                         MODIFY (7)                6                    │
    │                         WRITE (7)                 7  ← Correct!       │
    │                         release lock              7                    │
    │                                                                         │
    │   The "with lock:" statement handles acquire/release automatically.    │
    │                                                                         │
    │   ┌───────────────────────────────────────────────────────────────┐    │
    │   │                                                               │    │
    │   │   with self.lock:         is equivalent to:                  │    │
    │   │       # critical section      self.lock.acquire()            │    │
    │   │                               try:                           │    │
    │   │                                   # critical section         │    │
    │   │                               finally:                       │    │
    │   │                                   self.lock.release()        │    │
    │   │                                                               │    │
    │   └───────────────────────────────────────────────────────────────┘    │
    │                                                                         │
    └─────────────────────────────────────────────────────────────────────────┘
    """
    
    def __init__(self):
        self.count = 0
        self.lock = threading.Lock()
    
    def increment(self):
        # The lock ensures only ONE thread executes this block at a time
        #
        #   ┌───────────────────────────────────────────────────────────┐
        #   │  SAFE: Lock protects the critical section                │
        #   │                                                           │
        #   │  with self.lock:         ← Acquire lock (or wait)        │
        #   │      current = self.count                                │
        #   │      current = current + 1    ALL THREE STEPS            │
        #   │      self.count = current     ARE PROTECTED!             │
        #   │                          ← Release lock (automatic)      │
        #   │                                                           │
        #   │  Other threads CANNOT enter until lock is released.      │
        #   │                                                           │
        #   └───────────────────────────────────────────────────────────┘
        #
        with self.lock:
            current = self.count
            time.sleep(0.0001)
            self.count = current + 1


def demo_race_condition():
    """
    Show race condition and how locks fix it.
    
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                    RACE CONDITION DEMONSTRATION                         │
    ├─────────────────────────────────────────────────────────────────────────┤
    │                                                                         │
    │   EXPERIMENT:                                                          │
    │   ───────────                                                          │
    │   • Create 100 threads                                                 │
    │   • Each thread increments counter once                                │
    │   • Expected final value: 100                                          │
    │                                                                         │
    │   WITHOUT LOCK:                         WITH LOCK:                     │
    │   ─────────────                         ──────────                     │
    │                                                                         │
    │   Thread 1 ─┐                           Thread 1 ─┐                    │
    │   Thread 2 ─┤                           Thread 2 ─┤                    │
    │   Thread 3 ─┼──► counter ──► 87?!       Thread 3 ─┼──► 🔒 ──► 100 ✓  │
    │   ...       │    (lost updates!)        ...       │                    │
    │   Thread 100┘                           Thread 100┘                    │
    │                                                                         │
    │   Result: ~85-95 (varies each run!)     Result: ALWAYS 100            │
    │                                                                         │
    └─────────────────────────────────────────────────────────────────────────┘
    """
    print("\n" + "="*60)
    print("Demo 2: Race Condition vs Thread-Safe Counter")
    print("="*60)
    
    print("""
    ┌────────────────────────────────────────────────────────────┐
    │  EXPERIMENT: 100 threads, each increments counter once     │
    │  EXPECTED RESULT: 100                                      │
    └────────────────────────────────────────────────────────────┘
    """)
    
    # ─────────────────────────────────────────────────────────────────────────
    # Test UNSAFE counter (no lock)
    # ─────────────────────────────────────────────────────────────────────────
    print("[Testing UNSAFE counter (no lock)]")
    unsafe = UnsafeCounter()
    threads = []
    
    for _ in range(100):
        t = threading.Thread(target=unsafe.increment)
        threads.append(t)
        t.start()
    
    for t in threads:
        t.join()
    
    print(f"  Unsafe counter result: {unsafe.count}")
    print(f"  Expected: 100")
    print(f"  Status: {'✗ RACE CONDITION!' if unsafe.count != 100 else '✓ Lucky!'}")
    
    # ─────────────────────────────────────────────────────────────────────────
    # Test SAFE counter (with lock)
    # ─────────────────────────────────────────────────────────────────────────
    print("\n[Testing SAFE counter (with lock)]")
    safe = SafeCounter()
    threads = []
    
    for _ in range(100):
        t = threading.Thread(target=safe.increment)
        threads.append(t)
        t.start()
    
    for t in threads:
        t.join()
    
    print(f"  Safe counter result: {safe.count}")
    print(f"  Expected: 100")
    print(f"  Status: {'✓ CORRECT!' if safe.count == 100 else '✗ Unexpected!'}")
    
    print("""
    ┌────────────────────────────────────────────────────────────┐
    │  LESSON: Always protect shared mutable data with locks!   │
    └────────────────────────────────────────────────────────────┘
    """)


# =============================================================================
# Demo 3: Avoiding Blocking (Why Threads?)
# =============================================================================

def blocking_io_task(task_id):
    """
    Simulate a blocking I/O operation (e.g., network request, file read).
    
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                    BLOCKING I/O SIMULATION                              │
    ├─────────────────────────────────────────────────────────────────────────┤
    │                                                                         │
    │   Real-world blocking operations:                                      │
    │   • HTTP requests to APIs                                              │
    │   • Database queries                                                   │
    │   • File reads/writes                                                  │
    │   • Socket operations                                                  │
    │                                                                         │
    │   During I/O, the CPU is IDLE waiting for external response:           │
    │                                                                         │
    │   ┌─────────────────────────────────────────────────────────────┐      │
    │   │                                                             │      │
    │   │   Task:   ████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░████     │      │
    │   │                │                                  │         │      │
    │   │                │                                  │         │      │
    │   │           Start I/O                          I/O Complete   │      │
    │   │                │◄────── CPU IDLE HERE! ──────►│             │      │
    │   │                                                             │      │
    │   │   With threads, OTHER threads can use the CPU while        │      │
    │   │   this one waits!                                          │      │
    │   │                                                             │      │
    │   └─────────────────────────────────────────────────────────────┘      │
    │                                                                         │
    └─────────────────────────────────────────────────────────────────────────┘
    """
    print(f"  Task {task_id}: Starting I/O...")
    time.sleep(1)  # Simulate 1 second of I/O wait
    print(f"  Task {task_id}: I/O complete")
    return f"Result {task_id}"


def demo_avoid_blocking():
    """
    Show how threads avoid blocking.
    
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                    SEQUENTIAL vs CONCURRENT EXECUTION                   │
    ├─────────────────────────────────────────────────────────────────────────┤
    │                                                                         │
    │   SEQUENTIAL (single-threaded):                                        │
    │   ─────────────────────────────                                        │
    │                                                                         │
    │   Time: 0s        1s        2s        3s                               │
    │         │─────────│─────────│─────────│                                │
    │                                                                         │
    │   Task 1: ████████████                                                 │
    │   Task 2:             ████████████                                     │
    │   Task 3:                         ████████████                         │
    │                                                                         │
    │   Total: 3 seconds (tasks run one after another)                       │
    │                                                                         │
    │   ─────────────────────────────────────────────────────────────────────│
    │                                                                         │
    │   CONCURRENT (multi-threaded):                                         │
    │   ────────────────────────────                                         │
    │                                                                         │
    │   Time: 0s        1s                                                   │
    │         │─────────│                                                    │
    │                                                                         │
    │   Task 1: ████████████                                                 │
    │   Task 2: ████████████  ← All start at the same time!                 │
    │   Task 3: ████████████                                                 │
    │                                                                         │
    │   Total: 1 second (tasks run in parallel during I/O wait)             │
    │                                                                         │
    │   SPEEDUP: 3x faster!                                                  │
    │                                                                         │
    └─────────────────────────────────────────────────────────────────────────┘
    """
    print("\n" + "="*60)
    print("Demo 3: Avoiding Blocking with Threads")
    print("="*60)
    
    tasks = list(range(6))
    
    # ─────────────────────────────────────────────────────────────────────────
    # Sequential (blocking) execution
    # ─────────────────────────────────────────────────────────────────────────
    print("""
    ┌────────────────────────────────────────────────────────────┐
    │  SEQUENTIAL EXECUTION (single-threaded)                    │
    │                                                            │
    │  Task 1: ████████████                                     │
    │  Task 2:             ████████████                         │
    │  Task 3:                         ████████████             │
    │          0s         1s         2s         3s              │
    └────────────────────────────────────────────────────────────┘
    """)
    
    start = time.time()
    
    results = []
    for task_id in tasks:
        results.append(blocking_io_task(task_id))
    
    sequential_time = time.time() - start
    print(f"\n  Sequential time: {sequential_time:.2f}s")
    
    # ─────────────────────────────────────────────────────────────────────────
    # Concurrent (threaded) execution
    # ─────────────────────────────────────────────────────────────────────────
    print("""
    ┌────────────────────────────────────────────────────────────┐
    │  CONCURRENT EXECUTION (multi-threaded)                     │
    │                                                            │
    │  Task 1: ████████████                                     │
    │  Task 2: ████████████                                     │
    │  Task 3: ████████████                                     │
    │          0s         1s                                     │
    └────────────────────────────────────────────────────────────┘
    """)
    
    start = time.time()
    
    with ThreadPoolExecutor(max_workers=3) as executor:
        results = list(executor.map(blocking_io_task, tasks))
    
    concurrent_time = time.time() - start
    print(f"\n  Concurrent time: {concurrent_time:.2f}s")
    
    print(f"""
    ┌────────────────────────────────────────────────────────────┐
    │  RESULTS:                                                  │
    │                                                            │
    │  Sequential: {sequential_time:.2f}s                                        │
    │  Concurrent: {concurrent_time:.2f}s                                        │
    │  Speedup:    {sequential_time/concurrent_time:.1f}x faster!                                   │
    │                                                            │
    │  This is why we use threads for I/O-bound tasks!          │
    └────────────────────────────────────────────────────────────┘
    """)


# =============================================================================
# Demo 4: Thread Context (What's Shared, What's Not)
# =============================================================================

# Global variable - shared by all threads
global_var = "I am global"


def demo_thread_context():
    """
    Show what's shared vs thread-local.
    
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                    THREAD MEMORY MODEL                                  │
    ├─────────────────────────────────────────────────────────────────────────┤
    │                                                                         │
    │   ┌─────────────────────────────────────────────────────────────────┐   │
    │   │                     SHARED (Dangerous!)                         │   │
    │   │                                                                 │   │
    │   │   • Global variables     global_var = "hello"                  │   │
    │   │   • Module-level vars    counter = 0                           │   │
    │   │   • Mutable arguments    shared_list = []                      │   │
    │   │   • Object attributes    self.data = {}                        │   │
    │   │   • Class variables      MyClass.class_var = 42                │   │
    │   │                                                                 │   │
    │   │   ⚠ Changes visible to ALL threads!                            │   │
    │   │   ⚠ Must use locks to prevent race conditions!                 │   │
    │   │                                                                 │   │
    │   └─────────────────────────────────────────────────────────────────┘   │
    │                                                                         │
    │   ┌─────────────────────────────────────────────────────────────────┐   │
    │   │                     PRIVATE (Safe)                              │   │
    │   │                                                                 │   │
    │   │   • Local variables      def func(): x = 5                     │   │
    │   │   • Function parameters  def func(arg): ...                    │   │
    │   │   • Thread-local storage threading.local()                     │   │
    │   │                                                                 │   │
    │   │   ✓ Each thread has its own copy                               │   │
    │   │   ✓ No synchronization needed                                  │   │
    │   │                                                                 │   │
    │   └─────────────────────────────────────────────────────────────────┘   │
    │                                                                         │
    │   THREAD-LOCAL STORAGE (threading.local):                              │
    │   ────────────────────────────────────────                             │
    │                                                                         │
    │   thread_local = threading.local()                                     │
    │                                                                         │
    │   ┌───────────────┐  ┌───────────────┐  ┌───────────────┐             │
    │   │   Thread 0    │  │   Thread 1    │  │   Thread 2    │             │
    │   │               │  │               │  │               │             │
    │   │ thread_local  │  │ thread_local  │  │ thread_local  │             │
    │   │   .value = 0  │  │   .value = 1  │  │   .value = 2  │             │
    │   │               │  │               │  │               │             │
    │   │ (separate!)   │  │ (separate!)   │  │ (separate!)   │             │
    │   └───────────────┘  └───────────────┘  └───────────────┘             │
    │                                                                         │
    │   Same variable name, but each thread sees its own value!              │
    │                                                                         │
    └─────────────────────────────────────────────────────────────────────────┘
    """
    print("\n" + "="*60)
    print("Demo 4: Thread Context - Shared vs Local")
    print("="*60)
    
    print("""
    ┌────────────────────────────────────────────────────────────┐
    │  MEMORY TYPES IN THREADED PROGRAMS:                        │
    │                                                            │
    │  SHARED:           PRIVATE:                                │
    │  • Global vars     • Local vars (stack)                    │
    │  • Heap objects    • Thread-local storage                  │
    │  • Class vars      • Function parameters                   │
    └────────────────────────────────────────────────────────────┘
    """)
    
    # Thread-local storage - each thread gets its own copy
    thread_local = threading.local()
    
    def thread_function(thread_id):
        global global_var
        
        # ─────────────────────────────────────────────────────────────────
        # Thread-local variable (each thread has its own copy)
        # ─────────────────────────────────────────────────────────────────
        thread_local.value = f"Local to thread {thread_id}"
        
        # ─────────────────────────────────────────────────────────────────
        # Local variable (on thread's stack - private)
        # ─────────────────────────────────────────────────────────────────
        local_var = f"Stack variable in thread {thread_id}"
        
        print(f"\n[Thread {thread_id}]")
        print(f"  Global (shared):      '{global_var}'")
        print(f"  Thread-local:         '{thread_local.value}'")
        print(f"  Stack local:          '{local_var}'")
        
        # ─────────────────────────────────────────────────────────────────
        # Modify global - affects all threads!
        # ─────────────────────────────────────────────────────────────────
        if thread_id == 1:
            global_var = "Modified by thread 1"
            print(f"  → Thread 1 modified global_var!")
    
    # Run threads sequentially for clear demonstration
    threads = []
    for i in range(3):
        t = threading.Thread(target=thread_function, args=(i,))
        threads.append(t)
        t.start()
        t.join()  # Wait for each to complete before starting next
    
    print(f"\nFinal global_var: '{global_var}'")
    
    print("""
    ┌────────────────────────────────────────────────────────────┐
    │  SUMMARY:                                                  │
    │                                                            │
    │  • Global variables:       SHARED (dangerous!)             │
    │  • Thread-local storage:   SEPARATE per thread (safe)      │
    │  • Stack variables:        SEPARATE per thread (safe)      │
    │                                                            │
    │  Use threading.local() when you need per-thread data      │
    │  that should NOT be shared between threads.                │
    └────────────────────────────────────────────────────────────┘
    """)


# =============================================================================
# Main
# =============================================================================

if __name__ == "__main__":
    print("""
╔══════════════════════════════════════════════════════════════════════════════╗
║                         THREADS DEMONSTRATION                                ║
║                         Process Concepts - Part 1                            ║
╚══════════════════════════════════════════════════════════════════════════════╝

    This demo covers:

    ┌─────────────────────────────────────────────────────────────────────┐
    │                                                                     │
    │  Demo 1: Basic Thread Creation                                     │
    │          └── Creating threads, shared memory                       │
    │                                                                     │
    │  Demo 2: Race Conditions                                           │
    │          └── Why they happen, how locks prevent them               │
    │                                                                     │
    │  Demo 3: Avoiding Blocking                                         │
    │          └── Concurrent I/O, speedup measurement                   │
    │                                                                     │
    │  Demo 4: Thread Context                                            │
    │          └── Shared vs private memory                              │
    │                                                                     │
    └─────────────────────────────────────────────────────────────────────┘
    """)
    
    demo_basic_threads()
    demo_race_condition()
    demo_avoid_blocking()
    demo_thread_context()
    
    print("""
╔══════════════════════════════════════════════════════════════════════════════╗
║                            KEY TAKEAWAYS                                     ║
╚══════════════════════════════════════════════════════════════════════════════╝

    ┌─────────────────────────────────────────────────────────────────────┐
    │                                                                     │
    │  1. THREADS SHARE MEMORY                                           │
    │     └── Fast communication, but dangerous without synchronization  │
    │                                                                     │
    │  2. RACE CONDITIONS occur without proper synchronization           │
    │     └── Multiple threads modifying same data = unpredictable      │
    │                                                                     │
    │  3. LOCKS (mutexes) protect critical sections                      │
    │     └── Only one thread can hold the lock at a time               │
    │                                                                     │
    │  4. THREADS AVOID BLOCKING                                         │
    │     └── Perfect for I/O-bound tasks (network, disk, database)     │
    │                                                                     │
    │  5. USE THREAD-LOCAL for per-thread data                          │
    │     └── threading.local() gives each thread its own copy          │
    │                                                                     │
    └─────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────────────────────────┐
    │  WHEN TO USE THREADS vs PROCESSES:                                 │
    ├─────────────────────────────────────────────────────────────────────┤
    │                                                                     │
    │  THREADS (threading module):                                       │
    │    ✓ I/O-bound tasks (network, files, databases)                  │
    │    ✓ GUI applications (keep UI responsive)                        │
    │    ✓ Need fast communication between workers                      │
    │                                                                     │
    │  PROCESSES (multiprocessing module):                               │
    │    ✓ CPU-bound tasks (heavy computation)                          │
    │    ✓ Need true parallelism (bypass Python GIL)                    │
    │    ✓ Want isolation (crashes don't affect other workers)          │
    │                                                                     │
    └─────────────────────────────────────────────────────────────────────┘
    """)